{
  "description": "Single source of truth for boolean capability flags. Run 'node tools/add-capability.js <flagName> \"description\"' to add a new one.",
  "providerFlags": [
    "hasIssueTypes",
    "hasPriorities",
    "hasAssignees",
    "hasDueDate",
    "hasParentIssue",
    "hasAttachments",
    "hasComments",
    "hasSubtasks",
    "hasStatusTransitions",
    "hasAiWorkBreakdown",
    "hasCommentReplies"
  ],
  "routeFlags": ["hasSites", "hasSetupWizard"],
  "stringFlags": {
    "apiStyle": {
      "values": ["rest", "graphql"],
      "default": "rest",
      "description": "HTTP transport style. Use 'graphql' for platforms with a GraphQL-only API (monday.com, Linear). Generator emits createGraphQLClient from @mp/shared instead of the axios client."
    },
    "dueDateDisplay": {
      "values": ["date", "datetime"],
      "default": "date",
      "description": "Read-only due date display in shared UI. Use 'datetime' when the platform API returns due dates with time (e.g. Wrike dates.due)."
    }
  },
  "comments": {
    "providerFlags": "Exposed in PlatformCapabilities. UI components gate on these via usePlatformCapabilities(). Regenerate capability providers with: nx run <platform>:sync-capabilities",
    "routeFlags": "Affect route generation only (e.g. sites picker). NOT in PlatformCapabilities and NOT visible to UI components."
  }
}
