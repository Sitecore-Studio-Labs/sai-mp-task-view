"use client";

import { ActiveScopeCard } from "./ActiveScopeCard";

/** @deprecated Use ActiveScopeCard — kept for backward compatibility. */
export function ProjectSiteCard() {
  return <ActiveScopeCard />;
}
