export class <%= className %>AuthError extends Error {
  constructor(
    message = "<%= platformDisplay %> session has expired. Please reconnect <%= platformDisplay %>.",
  ) {
    super(message);
    this.name = "<%= className %>AuthError";
  }
}
